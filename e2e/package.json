{
	"name": "antiprocrastination-e2e",
	"version": "1.0.0",
	"private": true,
	"scripts": {
		"test": "playwright test",
		"test:ui": "playwright test --ui",
		"test:headed": "playwright test --headed",
		"codegen": "playwright codegen https://localhost:3000"
	},
	"devDependencies": {
		"@playwright/test": "^1.50.0",
		"dotenv": "^16.4.0"
	}
}
